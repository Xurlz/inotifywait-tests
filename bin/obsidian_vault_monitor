#!/usr/bin/env sh

inotifywait -m \
  -e create \
  -e close_write \
  -e move \
  --format "sleep 2; hit add .; hit commit -m '%T %e %f'" --timefmt "%F %T" \
  $(dirname $(realpath $0))/../share/obsidian/vault/{,.}**  | \
  sh

